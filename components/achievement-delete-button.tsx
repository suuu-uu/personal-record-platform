"use client";
import { Trash2 } from "lucide-react";import { useRouter } from "next/navigation";import { toast } from "sonner";import { AlertDialog,AlertDialogAction,AlertDialogCancel,AlertDialogContent,AlertDialogDescription,AlertDialogFooter,AlertDialogHeader,AlertDialogTitle,AlertDialogTrigger } from "@/components/ui/alert-dialog";
export function AchievementDeleteButton({id,title,afterDelete}:{id:number;title:string;afterDelete?:"list"}){const router=useRouter();async function remove(){try{const response=await fetch(`/api/achievements/${id}`,{method:"DELETE"});const data=await response.json() as {error?:string};if(!response.ok)throw new Error(data.error||"删除失败");toast.success("成就已删除");window.dispatchEvent(new Event("achievement:changed"));if(afterDelete==="list")router.push("/achievements");else router.refresh()}catch(error){toast.error(error instanceof Error?error.message:"删除失败")}}return <AlertDialog><AlertDialogTrigger asChild><button className="danger-button"><Trash2 size={16}/>删除</button></AlertDialogTrigger><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>确认删除“{title}”？</AlertDialogTitle><AlertDialogDescription>删除后无法恢复，关联待办会保留但不再关联此成就。</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>取消</AlertDialogCancel><AlertDialogAction variant="destructive" onClick={remove}>删除</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog>}
