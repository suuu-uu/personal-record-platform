"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { useState } from "react";
export function RegisterForm(){const router=useRouter(),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});const d=await r.json().catch(()=>({}));setBusy(false);if(!r.ok){setError(d.error||"注册失败");return}router.replace("/");router.refresh()}return <form className="login-form" onSubmit={submit}><div className="login-bow">୨୧</div><h2>创建你的空间</h2><h1>从今天开始记录。</h1><p>每个账号都有独立的个人档案。</p>{error&&<div className="login-error">{error}</div>}{[["username","用户名","text"],["email","邮箱","email"],["password","密码","password"],["confirmPassword","确认密码","password"]].map(([n,l,t])=><label className="login-field" key={n}>{l}<input name={n} type={t} className="field-input" required minLength={n.includes("password")?8:undefined}/></label>)}<button className="primary-button login-submit" disabled={busy}>{busy?"正在创建…":"创建账号"} →</button><p>已有账号？<Link href="/login">去登录</Link></p></form>}
