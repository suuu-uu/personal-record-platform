import { z } from "zod";
const attachmentSchema = z.object({ fileName:z.string().max(255),fileUrl:z.string().max(1000),fileType:z.string().max(160),fileSize:z.number().int().positive().max(20*1024*1024),isCover:z.boolean().default(false),sortOrder:z.number().int().min(0) });
export const achievementSchema=z.object({title:z.string().trim().min(1,"请输入成就名称").max(120),type:z.enum(["项目","比赛","实习","活动","证书","其他"]),role:z.string().max(120).optional().default(""),organization:z.string().max(160).optional().default(""),startDate:z.string().date("开始日期格式不正确"),endDate:z.string().optional().default(""),description:z.string().max(4000).optional().default(""),awardDetails:z.string().max(1000).optional().default(""),link:z.string().optional().default("").refine(value=>{if(!value)return true;try{new URL(value);return true}catch{return false}},"链接格式不正确"),tags:z.array(z.string().max(40)).max(20).default([]),imageUrl:z.string().max(3_000_000).optional().default(""),coverImageUrl:z.string().max(1000).optional().default(""),attachments:z.array(attachmentSchema).max(20).default([]),isPublic:z.boolean().default(false),isPinned:z.boolean().default(false)}).refine(value=>!value.endDate||value.endDate>=value.startDate,{message:"结束日期不能早于开始日期",path:["endDate"]});
export function achievementData(value:z.infer<typeof achievementSchema>){return{title:value.title,type:value.type,role:value.role||null,organization:value.organization||null,startDate:new Date(`${value.startDate}T00:00:00`),endDate:value.endDate?new Date(`${value.endDate}T00:00:00`):null,description:value.description||null,awardDetails:value.awardDetails||null,link:value.link||null,tags:JSON.stringify(value.tags),imageUrl:value.imageUrl||null,coverImageUrl:value.coverImageUrl||null,isPublic:value.isPublic,isPinned:value.isPinned}}
