"use client";import { useEffect } from "react";export default function ErrorPage({error,reset}:{error:Error & {digest?:string};reset:()=>void}){useEffect(()=>{fetch("/api/errors",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:error.message,stack:error.stack,path:location.pathname})}).catch(()=>{})},[error]);return <main className="main-panel"><p className="eyebrow">LOAD ERROR</p><h1>页面没有加载成功</h1><p>数据暂时无法读取，请稍后重试。</p><button className="primary-button" onClick={reset}>重新加载</button></main>}
