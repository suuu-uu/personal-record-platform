import { NextResponse } from "next/server";import { prisma } from "@/lib/prisma";import { todoData,todoSchema } from "@/lib/todo-validation";
export async function POST(request:Request){try{const parsed=todoSchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:parsed.error.issues[0]?.message||"表单内容不完整"},{status:400});const item=await prisma.todo.create({data:{...todoData(parsed.data),status:"todo",completedAt:null,isArchived:false,archivedAt:null}});return NextResponse.json(item,{status:201})}catch{return NextResponse.json({error:"保存待办失败，请稍后重试"},{status:500})}}
