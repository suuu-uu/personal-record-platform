# 个人网站平台需求

## 目标

建立一个仅管理员可访问的个人记录平台，用于管理成就、待办和足迹。桌面与移动端均可使用，数据保存在 SQLite。

## 页面

- `/login`：管理员登录。
- `/`：像素欢迎区、个人简介、统计、最近记录、足迹占位和公开成就时间轴。
- `/achievements`：按类型、年份和标签筛选，支持搜索、排序、新增、编辑和删除成就。
- `/todos`：搜索、新增、编辑、删除待办，勾选完成事项，手动清理，并每两个月自动软归档已完成事项。
- `/footprints`：世界地图、中国省份视图、新增和删除足迹。
- `/settings`：查看认证、数据和显示设置说明。
- `/admin`：业务管理入口。
- `/achievements/[id]`：完整成就详情，以及编辑和删除操作。

## 数据模型

- `Achievement`：标题、类型、角色、机构、起止日期、描述、奖项、链接、标签、图片、公开与置顶状态和时间戳。
- `Todo`：标题、描述、截止日期、优先级、状态、完成与归档时间、标签、关联成就、公开状态和时间戳。
- `Footprint`：唯一键、范围、国家与省份中英文名称及代码、到访状态、日期、备注和时间戳。

## 安全

除登录页和登录接口外，所有页面与接口都由服务端中间件验证 httpOnly JWT Cookie。账号密码只从服务端环境变量读取。
